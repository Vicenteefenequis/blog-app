export default class User {
    constructor(title, description, autor, imagem, history) {
      this.title = title;
      this.description = description;
      this.autor = autor;
      this.imagem = imagem;
      this.history = history;
    }
  }
  